# Personal overrides for Find a Job. This file is intentionally gitignored after
# copying it to config.local.yaml, so pulling updates never overwrites your settings.
#
# Uncomment and adjust only the values you want to override.

# llm:
#   provider: deepseek

# resumes:
#   # Default outside a source checkout: Find a Job's application-data directory.
#   # Optional override, for example: /path/to/generated-resumes

# scheduler:
#   enabled: true
